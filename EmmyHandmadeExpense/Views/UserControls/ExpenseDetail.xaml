<UserControl x:Class="AssetManager.Views.UserControls.ExpenseDetail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetManager.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="500"
             xmlns:vm="clr-namespace:AssetManager.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ExpenseDetailViewModel}"
             Background="Gainsboro">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="1"/>
            </Style>
        </Grid.Resources>

        <StackPanel Grid.ColumnSpan="99" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Load expenses between "/>
            <DatePicker SelectedDate="{Binding DateOne, Mode=TwoWay}" />
            <TextBlock Text=" and "/>
            <DatePicker SelectedDate="{Binding DateTwo, Mode=TwoWay}" />
            <Button Content="Search" Command="{Binding SearchCommand}" Padding="2" Margin="2,0"/>
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="0,01,0,1" Margin="0,1" Padding="1">
            <Border.BorderBrush>
                <LinearGradientBrush>
                    <GradientStop Color="Transparent" Offset="0.0"/>
                    <GradientStop Color="Red" Offset="0.2"/>
                    <GradientStop Color="Black" Offset="0.5"/>
                    <GradientStop Color="Red" Offset="0.8"/>
                    <GradientStop Color="Transparent" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="1" Text="Purchases:"/>
                <TextBox Grid.Column="1" Grid.Row="1" IsReadOnly="True" MinWidth="80" Text="{Binding PurchasesTotal, Mode=OneWay, StringFormat=0.00####}"/>

                <TextBlock Grid.Column="3" Grid.Row="1" Text="Tax:"/>
                <TextBox Grid.Column="4" Grid.Row="1" IsReadOnly="True" MinWidth="80" Text="{Binding TaxTotal, Mode=OneWay, StringFormat=0.00####}"/>

                <TextBlock Grid.Column="6" Grid.Row="1" Text="Discounts:"/>
                <TextBox Grid.Column="7" Grid.Row="1" IsReadOnly="True" MinWidth="80" Text="{Binding DiscountTotal, Mode=OneWay, StringFormat=0.00####}"/>

                <TextBlock Grid.Column="0" Grid.Row="2" Text="Shipping:"/>
                <TextBox Grid.Column="1" Grid.Row="2" IsReadOnly="True" MinWidth="80" Text="{Binding ShippingTotal, Mode=OneWay, StringFormat=0.00####}"/>

                <TextBlock Grid.Column="3" Grid.Row="2" Text="Misc:"/>
                <TextBox Grid.Column="4" Grid.Row="2" IsReadOnly="True" MinWidth="80" Text="{Binding MiscTotal, Mode=OneWay, StringFormat=0.00####}"/>

                <TextBlock Grid.Column="6" Grid.Row="2" Text="Sales Total:"/>
                <TextBox Grid.Column="7" Grid.Row="2" IsReadOnly="True" MinWidth="80" Text="{Binding SalesTotal, Mode=OneWay, StringFormat=0.00####}"/>

                <TextBlock Grid.Column="4" Grid.ColumnSpan="3" Grid.Row="3" Text="Total Expenditure:" HorizontalAlignment="Right"/>
                <TextBox Grid.Column="7" Grid.Row="3" IsReadOnly="True" MinWidth="80" Text="{Binding GrandTotal, Mode=OneWay, StringFormat=0.00####}"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="End-of-Period Values:"/>

            <TextBlock Grid.Column="2" Text="Materials:"/>
            <TextBox Grid.Column="3" IsReadOnly="True" MinWidth="80" Text="{Binding EOPMaterialTotal, Mode=OneWay}"/>

            <TextBlock Grid.Column="5" Text="Products:"/>
            <TextBox Grid.Column="6" IsReadOnly="True" MinWidth="80" Text="{Binding EOPProductTotal, Mode=OneWay}"/>
        </Grid>
    </Grid>
</UserControl>
