<UserControl x:Class="AssetManager.Views.UserControls.QuarterlyExpense"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetManager.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400"
             xmlns:vm="clr-namespace:AssetManager.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:QuarterlyExpenseViewModel}">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
        <TextBlock VerticalAlignment="Center" Text="Year:"/>
        <ComboBox Margin="4" ItemsSource="{Binding AvailableYears}"
                  SelectedValue="{Binding SelectedYear}" />
        <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="Quarter:"/>
        <ComboBox Margin="4" ItemsSource="{Binding AvailableQuarters}"
                  SelectedValue="{Binding SelectedQuarter}" />
        <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="Expenses:"/>
        <TextBox Margin="8,0,0,0" IsReadOnly="True" Text="{Binding ExpenseTotal, Mode=OneWay, StringFormat=N2}"
                 HorizontalContentAlignment="Right" VerticalAlignment="Center" MinWidth="60"/>
        <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="Sales:"/>
        <TextBox Margin="8,0,0,0" IsReadOnly="True" Text="{Binding SalesTotal, Mode=OneWay, StringFormat=N2}"
                 HorizontalContentAlignment="Right" VerticalAlignment="Center" MinWidth="60"/>
    </StackPanel>
</UserControl>
